version: 2
updates:
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
    ignore:
      # Path dependencies — local vendor packages, not on PyPI
      - dependency-name: bfcl-eval
      - dependency-name: tau-bench
      - dependency-name: llm-sdk
  - package-ecosystem: uv
    directory: /llm_sdk
    schedule:
      interval: weekly
