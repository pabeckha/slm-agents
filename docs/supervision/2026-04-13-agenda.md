# Supervision Meeting Agenda — 2026-04-13

**Student**: Paulo Beckhauser (s242779)
**Supervisor**: Nicki Skafte Detlefsen
**Format**: Online
**Previous meeting**: 2026-03-16

---

## Progress (since 2026-03-16)

Phase 1 — no-retraining configs — is almost fully closed on BFCL simple_python.

| Config | Accuracy | Delta vs CD | Status |
|---|---|---|---|
| B (no guided decoding) | 1.5% | −71.25 pp | Done |
| CD (guided) | 72.75% | baseline | Done |
| CD+Q (AWQ INT4) | 72.0% | −0.75 pp | Done |
| PE (few-shot) | 70.25% | −2.5 pp (negative) | Done |
| CD+Q+ITC (CoT) | TBD | TBD | Running now |
| CD+Q+RAG | — | — | Next |

Tangible artifacts to show:

- Config decision docs in `docs/decisions/` — one per config, with failure-mode tables
- Frontier baselines (GPT + Claude) running on BFCL for the ceiling comparison
- vLLM + BFCL harness with structured run manifests + reasoning-trace capture
- HPC infrastructure stable on both `gpua100` and `gpul40s`

Headlines:

- **Quantization is effectively free** (AWQ INT4 costs 0.75 pp and saves 63.5% VRAM) — validates using the quantized model as the baseline for all subsequent configs.
- **Prompt engineering did not help** (−2.5 pp) — the failure modes are learned associations, not format gaps.
- **Config CD+Q+ITC (CoT) is running right now** on gpul40s. Results filled in Monday morning before the meeting.

---

## Problems / questions for Nicki

1. **Evidence for LoRA as the answer.** Two prompt-only interventions (PE confirmed negative; CD+Q+ITC TBD) have now been tested. If CD+Q+ITC also regresses, is that strong enough evidence in a thesis to commit to LoRA as *the* solution for RQ2's argument-extraction failure class, or should I run a third prompt-only technique first?

2. **ReAct scope on Phase 1.** On BFCL simple_python, ReAct structurally degenerates to CoT (one function, no observation step, no iteration). Do you agree that Phase 1 can defend "ITC ≈ CoT on single-call BFCL" as sufficient, or should τ-bench be pulled forward so ReAct gets its own distinct evaluation before LoRA?

3. **Ceiling gap.** The spec target is ≥85% AST accuracy, current best is 72.0%. The 13 pp gap has to come almost entirely from LoRA. Is that realistic for Qwen2.5-7B on the Glaive function-calling dataset, or would you recommend a different training source (Claude Opus distillation traces, BFCL train split, synthetic)?

4. **Breadth vs depth.** Stay on Qwen2.5-7B and go deep into training (LoRA → distillation → RAG on the same model), or add Phi-4 Mini / Llama 3.2 as breadth data points first (Issue #16)? Default plan is depth-first; want your vote.

5. **Writing cadence.** Phase 1 methods are now frozen. Can I start the Methodology chapter outline this week and bring a draft to the next meeting, or does writing wait until all experiments are done?

---

## Plan (next ~2 weeks)

- **This week**: Land CD+Q+ITC results doc; open PR. Run Config CD+Q+RAG (Issue #7).
- **Next week**: Prepare Phase 2 data — BFCL train/test splits + Glaive dataset formatting (Issue #9). Start Methodology chapter outline in parallel (pending question 5).
- **Milestone**: Phase 1 fully complete and documented by ~2026-04-20. First LoRA training run by ~2026-04-27.
- **Open issues that are not on the critical path**: #16 (extra SLMs), #4 (τ-bench harness), #24 (full ablation matrix). Flag if you think any of these should move up.

---

## Pre-read (optional)

If you have 10 minutes before the meeting, these are the highest-signal docs:

- `docs/decisions/config-pe-few-shot-results.md` — negative result, failure-mode analysis
- `docs/decisions/config-cdq-itc-spec.md` — why ReAct is out of scope for Phase 1
- `docs/planning/implementation-order.md` — current phase roadmap

---

## Logistics

- **Online meeting** — confirm link in the calendar invite.
- Agenda sent to Nicki **by Sunday 2026-04-12** so there is time to pre-read.
